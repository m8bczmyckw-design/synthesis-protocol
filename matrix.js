(()=>{const c=document.getElementById('matrixCanvas'),x=c.getContext('2d');let w,h,cols,drops;const chars='01アイウエオカキクケコサシスセソナー人機械記憶神経SYNTHESIS';function size(){w=c.width=innerWidth;h=c.height=innerHeight;cols=Math.floor(w/14);drops=Array.from({length:cols},()=>Math.random()*h)}addEventListener('resize',size);size();function draw(){x.fillStyle='rgba(0,0,0,.08)';x.fillRect(0,0,w,h);x.font='16px monospace';for(let i=0;i<cols;i++){const ch=chars[Math.floor(Math.random()*chars.length)];x.fillStyle=Math.random()>.98?'#dfffe9':'#00ff8a';x.fillText(ch,i*14,drops[i]);drops[i]+=14+Math.random()*8;if(drops[i]>h&&Math.random()>.965)drops[i]=0}requestAnimationFrame(draw)}draw()})();
